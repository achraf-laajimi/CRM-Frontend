// UserContextType.ts
export interface User {
    username: string;
    // Add other user properties if needed
}

export interface UserContextType {
    user?: User;
    // Add other context properties or methods if needed
}
